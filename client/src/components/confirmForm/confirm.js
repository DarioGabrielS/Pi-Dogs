import style from './confirm.module.css'

export const Confirm = (name, weight, height, life_span, temperament)=>{

    return(
        <div className={style.container}>
            
        </div>
    )
}